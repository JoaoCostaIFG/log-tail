name: Build daily edition of the volume

on:
  schedule:
    - cron: "23 8 * * *"
      timezone: 'Europe/Lisbon'
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths-ignore:
      - "site/**"

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore previous editions
        run: |
          mkdir -p site
          if git ls-remote --exit-code --heads origin site >/dev/null 2>&1; then
            git fetch --depth=1 origin site
            git archive origin/site | tar -x -C site
          fi

      - name: Build edition
        run: python build.py

      - name: Publish site branch
        run: |
          cd site
          git init -q -b site
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -qm "Edition $(date -u +%F)"
          git remote add origin "https://x-access-token:${{ github.token }}@github.com/${GITHUB_REPOSITORY}.git"
          git push -q --force origin site
